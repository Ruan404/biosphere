<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Films disponibles</title>
</head>
<body>
    <h1>Films disponibles :</h1>
    <?php
    // Lisez le fichier CSV
    $fichier_csv = 'infos.csv';
    $lignes = file($fichier_csv, FILE_IGNORE_NEW_LINES | FILE_SKIP_EMPTY_LINES);

    // Affichez chaque documentaire
    foreach ($lignes as $ligne) {
        $infos = explode(';', $ligne);
        $id = $infos[0];
        $nom = $infos[1];
        $genre = $infos[2];
        $description = $infos[3];
        $chemin_image = $infos[4];
        $chemin_film = $infos[5];

        echo '<h2>' . $nom . '</h2>';
        echo '<p>Genre : ' . $genre . '</p>';
        echo '<p>' . $description . '</p>';
        echo '<img src="' . $chemin_image . '" alt="' . $nom . '">';
        echo '<video controls>';
        echo '<source src="' . $chemin_film . '" type="video/mp4">';
        echo 'Votre navigateur ne prend pas en charge la lecture de vidéos.';
        echo '</video>';
    }
    ?>
</body>
</html>